# AIRMAN AI Dispatch Agent

A constraint-aware flight dispatch and dynamic reallocation system built using FastAPI, PostgreSQL, Redis, and LangGraph orchestration.

---

## 🚀 Quick Start

```bash
docker-compose up --build